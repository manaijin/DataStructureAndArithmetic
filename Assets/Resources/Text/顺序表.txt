	线性表的顺序存储是指用一组地址连续的存储单元依次存储线性表中的各个元素，使得线性表在逻辑结构上相邻的元素存储在连续的物理存储单元中。
	顺序存储的线性表的特点： 
◆ 线性表的逻辑顺序与物理顺序一致; 
◆ 数据元素之间的关系是以元素在计算机内“物理位置相邻”来体现。
	分析：
◆ 数组的优势，在于可以方便的遍历查找需要的数据。这种时间上的便利性，是因为数组在内存中占用了连续的空间，本质是指针在内存中的定向偏移。
◆ 然而，当需要对数组成员进行添加和删除的操作时，数组内完成这类操作的时间复杂度则变成了O(n)。